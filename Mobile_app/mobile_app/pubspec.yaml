name: urbifix
description: UrbiFix — instant home services and civic help
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^0.13.6
  flutter_map: ^4.0.0
  latlong2: ^0.8.2
  location: ^5.0.3
  shared_preferences: ^2.2.2

  image_picker: ^0.8.7+4
  cached_network_image: ^3.2.3
  intl: ^0.18.0
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
